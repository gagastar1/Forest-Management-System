# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["maven", "jdk"]

[phases.build]
cmds = ["cd backend && chmod +x mvnw && ./mvnw clean package -DskipTests"]

[phases.start]
cmd = "cd backend && java -Dserver.port=$PORT -jar target/forest-management-system-1.0.0.jar"
